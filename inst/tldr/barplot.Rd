% Generated by roxygen2: do not edit by hand
\name{barplot}
\alias{barplot}
\title{Create Bar Plots from pre-Computed Heights}
\arguments{
  \item{height}{a vector or matrix of values to be plotted as bars}
  \item{horiz}{logical; should the bars be displayed horizontally?}
  \item{beside}{logical; if TRUE, bars are plotted side-by-side rather than stacked}
  \item{legend.text}{character vector for legend labels (when using matrices)}
  \item{...}{\{.help [graphical parameters](graphics::par)\}, e.g. `col`, `border`
}
}

\details{
Create basic barplots from a pre-computed table:
\code{
(counts <- table(mtcars$cyl))
barplot(counts)
barplot(counts, horiz = TRUE, col = "steelblue")
}

Create stacked and dodged barplots from a 2d contingency table:
\code{
counts <- xtabs(~ cyl + gear, data = mtcars)
cols <- c("firebrick", "gray80", "steelblue")
barplot(counts, col = cols, xlab = "gear")
barplot(counts, col = cols, xlab = "gear", beside = TRUE)
legend("topright", legend = unique(mtcars$cyl), fill = cols, title = "cyl", horiz = TRUE)
}}

